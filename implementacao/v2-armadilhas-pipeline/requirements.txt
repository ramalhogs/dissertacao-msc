# LLM providers (o SDK openai atende openrouter/gemini/groq por base_url).
openai>=1.40.0
anthropic>=0.34.0

# Config.
python-dotenv>=1.0.0

# Necessarios apenas se for executar os pipelines gerados (nao exigido pela coleta).
# scikit-learn>=1.4.0
# pandas>=2.2.0
# numpy>=1.26.0
